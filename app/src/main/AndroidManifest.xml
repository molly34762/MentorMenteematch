<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.liangm2rpi.mentormenteematch">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".first_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".log_in" />
        <activity android:name=".sign_up" />
        <activity android:name=".main_app" />
        <activity android:name=".profile" />
        <activity android:name=".profile_clubs" />
        <activity android:name=".profile_major" />
        <activity android:name=".architecture_program" />
    </application>

</manifest>